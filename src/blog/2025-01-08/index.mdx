import { MdxWrapper } from '../../layout/MdxWrapper'
import { meta } from './meta'

export default ({ children }) => (
  <MdxWrapper meta={meta} type="blog">
    {children}
  </MdxWrapper>
)

Just like with work, this site development also requires some time management and prioritization. Except in this case the prioritization is around, well, life.

Hello, world, it's been a hot minute. Things are pretty bare bones at the moment, but hoping time will fix that.
